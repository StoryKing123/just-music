import { CircularProgress } from "@mui/material";

const CircularProgressWrapper = () => {
    return <CircularProgress style={{ color: "var(--color-text-base)" }} />;
};
export default CircularProgressWrapper;
